/*
*OpenKattis
*timeloop
*Code by: Amanda Lim
*/

#include <bits/stdc++.h>
using namespace std;

int main ()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    
    int N;
    cin >> N;
    for (int n = 1; n <= N; n++)
        cout << n << " Abracadabra" << endl;
    
    return 0;
}